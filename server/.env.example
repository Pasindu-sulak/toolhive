PORT=5000
CLIENT_URL=http://localhost:5173
# MongoDB connection string. Leave unset to run on the built-in JSON tool catalog only
# (contact form submissions will just be logged, not stored).
MONGODB_URI=mongodb://127.0.0.1:27017/toolhive
